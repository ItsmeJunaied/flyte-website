name: 🚀 Deploy website on push

on:
  push:
    branches:
      - dev

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 162.0.220.49
          username: junaied@fsl.flytesolutions.com
          password: junayedcpanel
